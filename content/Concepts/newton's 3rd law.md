#u2 #forces #newton
- (*def.*) whenever an **object exerts a [[force]] on another object**, the *second object also exerts a [[force]] on the first object*.
	- The [[force|forces]] the objects exert on each other are always equal in magnitude and opposite direction.